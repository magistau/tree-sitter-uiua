#!/usr/bin/env uiua run --no-format
⊃(°json&fras|°0&runi□₂"redo-ifchange") "./primitives.json"
⍚(insert"name") °map
⊕□ ⊸≡◇(has"modifier_args")
⍚(⊕□ ⊸◇≡⌞◇(°□get)) {"args" "modifier_args"}
⍚⍚⍚(map⟜get▽◡has{"name" "glyph" "ascii"})
&fwa°□⊡₃&args json map{"func" "macro"}
