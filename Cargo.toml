[package]
name = "tree-sitter-uiua"
description = "A stack-based array programming language"
version = "0.1.0"
authors = ["Tau <tree-sitter@alice-carroll.pet>"]
license = "BSD-3-Clause"
readme = "README.md"
keywords = ["incremental", "parsing", "tree-sitter", "uiua"]
categories = ["parser-implementations", "parsing", "text-editors"]
repository = "https://github.com/magistau/tree-sitter-uiua"
edition = "2021"
autoexamples = false

build = "bindings/rust/build.rs"
include = [
  "bindings/rust/*",
  "grammar.js",
  "queries/*",
  "src/*",
  "tree-sitter.json",
  "LICENSE",
]

[lib]
path = "bindings/rust/lib.rs"

[dependencies]
tree-sitter-language = "0.1"

[build-dependencies]
cc = "1.2"

[dev-dependencies]
tree-sitter = "0.25.3"
